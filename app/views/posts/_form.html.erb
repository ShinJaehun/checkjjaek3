<%= form_for [postable, Post.new] do |form| %>
  <%= form.text_area :content, id: "textarea", require: true, placeholder: '이 책을 읽고 무엇을 느꼈나요?' %>
  <%= form.submit '책짹', class: 'btn btn-info' %>
<% end %>
